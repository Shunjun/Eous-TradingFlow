/**
 * Content paths for Tailwind CSS class scanning.
 * Include these in your app's Tailwind source configuration
 * so that @eous/ui component classes are detected.
 */
export const contentPaths = [
  './src/**/*.{ts,tsx}',
  '../../packages/ui/src/**/*.{ts,tsx}',
] as const
